# ID: NP1-08

## Title

Performance budgets (bundle size + route critical JS)

## Context

Lighthouse and micro-benchmarks exist but bundle regressions can still slip. Add a strict budget check without runtime dependencies.

## Steps

1. Define bundle budgets (global + per critical route).
2. Add a script to parse `.next` build outputs and fail on regressions.
3. Integrate into CI extended tier.

## Acceptance Criteria

- CI fails with actionable output when budgets are exceeded.

## Dependencies

- None.

## Files to modify

- `scripts/quality/*`
- `.github/workflows/ci-core.yml`
- `docs/performance/*`
