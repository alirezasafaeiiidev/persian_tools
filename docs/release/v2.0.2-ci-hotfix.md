# PersianToolbox v2.0.2 - CI Hotfix

Date: 2026-02-12
Target domain: `https://persiantoolbox.ir`

## Summary

Patch release for CI reliability after `v2.0.1`.

- Fixed test-script detection in `.github/workflows/js-ts-level1.yml`:
  - `pnpm run test:unit` now runs only when `test:unit` exists as an exact script key.
  - Falls back correctly to `pnpm run test` in repositories that do not define `test:unit`.

## Verification Snapshot

- `pnpm run ci:quick` passed
- `pnpm run build` passed
- `pnpm run smoke:local` passed

## Notes

- No runtime behavior change for end users.
- No new dependency introduced.
